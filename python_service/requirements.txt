# PHASE 1: Basic Setup - Only essentials
# Install only what we need NOW

# FastAPI Web Framework (for Python service)
fastapi==0.104.1
uvicorn==0.24.0

# Environment variables
python-dotenv==1.0.0

# That's it for Phase 1!
# Total size: ~50MB
# We'll add more libraries in each phase as needed